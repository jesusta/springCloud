server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jesusta/springCloud.git
          searchPaths: config-data
          username: ${GIT_USERNAME}
          password: ${YOU_PASSWORD}
          default-label: "development"





