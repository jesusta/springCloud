server:
  port: 1922
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/biblioteca?serverTimezone=America/Bogota
    username: root
    password:
  jpa:
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/

Feign:
  circuitbreaker:
    enabled: true
  hystrix:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"

